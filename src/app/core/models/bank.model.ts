/**
 * Topbar company-selector model.
 */
export interface Bank {
  name: string;
  tagline: string;
  /** Logo circle background color. */
  accent: string;
  /** Short initials shown inside the logo circle. */
  initials: string;
}