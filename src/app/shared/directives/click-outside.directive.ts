import { Directive } from '@angular/core';

/**
 * Click-outside directive — stub. Fires nothing yet.
 */
@Directive({
  selector: '[appClickOutside]',
  standalone: true,
})
export class ClickOutsideDirective {}